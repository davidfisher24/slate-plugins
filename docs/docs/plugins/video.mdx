---
slug: /plugins/video
title: Video
---

### `createVideoPlugin`

```ts live
() => {
  const plugins = [
    ...pluginsBasic,
    createVideoPlugin(),
    createSelectOnBackspacePlugin({ allow: [ELEMENT_VIDEO] }),
  ];
  return (
    <>
    <HeadingToolbar>
      <ToolbarVideo icon={<OndemandVideo />} />
    </HeadingToolbar>
      <Plate
        id="video"
        plugins={plugins}
        components={components}
        options={options}
        editableProps={editableProps}
        initialValue={initialValueVideo}
      />
    </>
  );
}
```